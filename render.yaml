services:
  - type: web
    name: calendar-booking
    env: docker
    plan: free
    envVars:
      - key: DATABASE_PATH
        value: /var/data/booking.sqlite
    disk:
      name: sqlite-data
      mountPath: /var/data
      sizeGB: 1
